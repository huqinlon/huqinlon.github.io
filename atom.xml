<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://amiao.wawj.cool</id>
    <title>Gridea</title>
    <updated>2021-03-14T14:15:33.228Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://amiao.wawj.cool"/>
    <link rel="self" href="https://amiao.wawj.cool/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://amiao.wawj.cool/images/avatar.png</logo>
    <icon>https://amiao.wawj.cool/favicon.ico</icon>
    <rights>All rights reserved 2021, Gridea</rights>
    <entry>
        <title type="html"><![CDATA[Rclone 使用教程 - 常用命令参数]]></title>
        <id>https://amiao.wawj.cool/post/rclone-shi-yong-jiao-cheng-chang-yong-ming-ling-can-shu/</id>
        <link href="https://amiao.wawj.cool/post/rclone-shi-yong-jiao-cheng-chang-yong-ming-ling-can-shu/">
        </link>
        <updated>2021-03-14T13:55:04.000Z</updated>
        <content type="html"><![CDATA[<figure data-type="image" tabindex="1"><img src="https://i.loli.net/2021/02/28/9qCaB6nsNrpXgJT.jpg" alt="img" loading="lazy"></figure>
<h1 id="设置">设置</h1>
<table>
<thead>
<tr>
<th>命令</th>
<th><code>说明</code></th>
</tr>
</thead>
<tbody>
<tr>
<td>rclone config</td>
<td>添加、删除、管理网盘等操作</td>
</tr>
<tr>
<td>rclone config file</td>
<td>显示配置文件的路径</td>
</tr>
<tr>
<td>rclone config show</td>
<td>显示配置文件信息</td>
</tr>
</tbody>
</table>
<pre><code>root@SunPma:~# rclone config
Current remotes:

Name                 Type
====                 ====
DB                 dropbox
GD                 drive

e) Edit existing remote
n) New remote
d) Delete remote
r) Rename remote
c) Copy remote
s) Set configuration password
q) Quit config
e/n/d/r/c/s/q&gt;
</code></pre>
<h1 id="语法">语法</h1>
<pre><code># 本地到网盘
rclone [功能选项] &lt;本地路径&gt; &lt;配置名称:路径&gt; [参数] [参数]

# 网盘到本地
rclone [功能选项] &lt;配置名称:路径&gt; &lt;本地路径&gt; [参数] [参数]

# 网盘到网盘
rclone [功能选项] &lt;配置名称:路径&gt; &lt;配置名称:路径&gt; [参数] [参数]

# [参数]为可选项
</code></pre>
<p><strong>示例</strong></p>
<pre><code># 复制到网盘，并显示实时传输进度，设置并行上传数为8

rclone copy -P /home/SunPma GD:/home/SunPma --transfers=8

# 如果需要服务端对服务端的传输可加以下参数（不消耗本地流量）

rclone copy 配置名称:网盘路径 配置名称:网盘路径 --drive-server-side-across-configs
</code></pre>
<h1 id="功能">功能</h1>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>rclone copy</td>
<td>复制</td>
</tr>
<tr>
<td>rclone move</td>
<td>移动，如果要在移动后删除空源目录，加上 --delete-empty-src-dirs 参数</td>
</tr>
<tr>
<td>rclone sync</td>
<td>同步：将源目录同步到目标目录，只更改目标目录</td>
</tr>
<tr>
<td>rclone size</td>
<td>查看网盘文件占用大小</td>
</tr>
<tr>
<td>rclone delete</td>
<td>删除路径下的文件内容</td>
</tr>
<tr>
<td>rclone purge</td>
<td>删除路径及其所有文件内容</td>
</tr>
<tr>
<td>rclone mkdir</td>
<td>创建目录</td>
</tr>
<tr>
<td>rclone rmdir</td>
<td>删除目录</td>
</tr>
<tr>
<td>rclone rmdirs</td>
<td>删除指定环境下的空目录。如果加上 --leave-root 参数，则不会删除根目录</td>
</tr>
<tr>
<td>rclone check</td>
<td>检查源和目的地址数据是否匹配</td>
</tr>
<tr>
<td>rclone ls</td>
<td>列出指定路径下的所有的文件以及文件大小和路径</td>
</tr>
<tr>
<td>rclone lsl</td>
<td>比上面多一个显示上传时间</td>
</tr>
<tr>
<td>rclone lsd</td>
<td>列出指定路径下的目录</td>
</tr>
<tr>
<td>rclone lsf</td>
<td>列出指定路径下的目录和文件</td>
</tr>
</tbody>
</table>
<h1 id="参数">参数</h1>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>-n = --dry-run</td>
<td>测试运行，查看Rclon在实际运行中会进行哪些操作</td>
</tr>
<tr>
<td>-P = --progress</td>
<td>显示实时传输进度，500mS刷新一次，否则默认1分钟刷新一次</td>
</tr>
<tr>
<td>--cache-chunk-size SizeSuffi</td>
<td>块的大小，默认5M越大上传越快，占用内存越多，太大可能会导致进程中断</td>
</tr>
<tr>
<td>--cache-chunk-total-size SizeSuffix</td>
<td>块可以在本地磁盘上占用的总大小，默认10G</td>
</tr>
<tr>
<td>--transfers=N</td>
<td>并行文件数，默认为4</td>
</tr>
<tr>
<td>--config string</td>
<td>指定配置文件路径，string为配置文件路径</td>
</tr>
<tr>
<td>--ignore-errors</td>
<td>跳过错误</td>
</tr>
</tbody>
</table>
<h1 id="日志">日志</h1>
<p>有4个级别的日志记录：<code>ERROR</code> <code>NOTICE</code> <code>INFO</code> <code>DEBUG</code><br>
默认情况下<code>Rclon</code>将生成<code>ERROR</code> <code>NOTICE</code>日志</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>-q</td>
<td>rclone将仅生成ERROR消息</td>
</tr>
<tr>
<td>-v</td>
<td>rclone将生成ERROR NOTICE INFO 消息</td>
</tr>
<tr>
<td>-vv</td>
<td>rclone 将生成ERROR NOTICE INFO DEBUG 消息</td>
</tr>
<tr>
<td>--log-level LEVEL</td>
<td>标志控制日志级别</td>
</tr>
</tbody>
</table>
<p><strong>输出日志到文件</strong><br>
使用<code>--log-file=FILE</code>选项<code>rclone</code>会将<code>Error</code> <code>Info</code> <code>Debug</code>消息以及标准错误重定向到<code>FILE</code><br>
这里的<code>FILE</code>是你指定的日志文件路径</p>
<h1 id="过滤">过滤</h1>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>--exclude</td>
<td>排除文件或目录</td>
</tr>
<tr>
<td>--include</td>
<td>包含文件或目录</td>
</tr>
<tr>
<td>--filter</td>
<td>文件过滤规则，相当于上面两个选项的其它使用方式。包含规则以+开头，排除规则以-开头</td>
</tr>
</tbody>
</table>
<p><strong>文件类型过滤</strong><br>
比如<code>--exclude &quot;*.bak&quot; --filter &quot;- *.bak&quot;</code>排除所有<code>bak</code>文件<br>
比如<code>--include &quot;*.{png,jpg}&quot; --filter &quot;+ *.{png,jpg}&quot;</code>包含所有<code>png</code>和<code>jpg</code>文件，排除其他文件<br>
<code>--delete-excluded</code>删除排除的文件。需配合过滤参数使用，否则无效<br>
<strong>目录过滤</strong><br>
目录过滤需要在目录名称后面加上<code>/</code>否则会被当做文件进行匹配<br>
以<code>/</code>开头只会匹配根目录（指定目录下），否则匹配所目录，这同样适用于文件<br>
<code>--exclude &quot;.git/&quot;</code>排除所有目录下的<code>.git</code>目录<br>
<code>--exclude &quot;/.git/&quot;</code>只排除根目录下的<code>.git</code>目录<br>
<code>--exclude &quot;{Video,Software}/&quot;</code>排除所有目录下的<code>Video</code>和<code>Software</code>目录<br>
<code>--exclude &quot;/{Video,Software}/&quot;</code>只排除根目录下的<code>Video</code>和<code>Software</code>目录<br>
<code>--include &quot;/{Video,Software}/**&quot;</code>仅包含根目录下的<code>Video</code>和<code>Software</code>目录的所有内容<br>
<strong>大小过滤</strong><br>
默认大小单位为<code>kBytes</code>但可以使用<code>k</code> <code>M</code>或<code>G</code>后缀<br>
<code>--min-size</code>过滤小于指定大小的文件。比如<code>--min-size 50</code>表示不会传输小于<code>50k</code>的文件。<br>
<code>--max-size</code>过滤大于指定大小的文件。比如<code>--max-size 1G</code>表示不会传输大于<code>1G</code>的文件。<br>
<strong>实际使用中发现大小过滤两个选项不能同时使用</strong><br>
<strong>过滤规则文件</strong><br>
<code>--filter-from &lt;规则文件&gt;</code>从文件添加包含<code>/</code>排除规则<br>
比如<code>--filter-from filter-file.txt</code><br>
<strong>过滤规则文件示例：</strong></p>
<pre><code>- secret*.jpg
+ *.jpg
+ *.png
+ file2.avi
- /dir/Trash/**
+ /dir/**
- *
</code></pre>
<h1 id="环境变量">环境变量</h1>
<p><code>rclone</code>中的每个选项都可以通过环境变量设置。环境变量的名称可以通过长选项名称进行转换，删除<code>--</code>前缀，更改<code>-</code>为<code>_</code>大写并添加前缀<code>RCLONE_</code>环境变量的优先级会低于命令行选项，即通过命令行追加相应的选项时会覆盖环境变量设定的值。<br>
比如设置最小上传大小<code>--min-size 50</code>使用环境变量是<code>RCLONE_MIN_SIZE=50</code>当环境变量设置后，在命令行中使用<code>--min-size 100</code>那么此时环境变量的值就会被覆盖<br>
<strong>常用环境变量</strong></p>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>RCLONE_CONFIG</td>
<td>自定义配置文件路径</td>
</tr>
<tr>
<td>RCLONE_CONFIG_PASS</td>
<td>若 rclone 进行了加密设置，把此环境变量设置为密码，可自动解密配置文件</td>
</tr>
<tr>
<td>RCLONE_RETRIES</td>
<td>上传失败重试次数，默认 3 次</td>
</tr>
<tr>
<td>RCLONE_RETRIES_SLEEP</td>
<td>上传失败重试等待时间，默认禁用，单位s、m、h分别代表秒、分钟、小时</td>
</tr>
<tr>
<td>CLONE_TRANSFERS</td>
<td>并行上传文件数</td>
</tr>
<tr>
<td>RCLONE_CACHE_CHUNK_SIZE</td>
<td>块的大小，默认5M</td>
</tr>
<tr>
<td>RCLONE_CACHE_CHUNK_TOTAL_SIZE</td>
<td>块可以在本地磁盘上占用的总大小，默认10G</td>
</tr>
<tr>
<td>RCLONE_IGNORE_ERRORS=true</td>
<td>跳过错误</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
</feed>